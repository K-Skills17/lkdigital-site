# ─────────────────────────────────────────────────────────────
# LK Digital — Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git.
# ─────────────────────────────────────────────────────────────

# ── Leonardo AI (image generation) ───────────────────────────
# Get your key at: https://app.leonardo.ai → Settings → API
LEONARDO_API_KEY=

# Optional: override the generation model (default: Leonardo Kino XL)
# Popular IDs:
#   aa77f04e-3eec-4034-9c07-d0f619684628  Leonardo Kino XL (cinematic, default)
#   b24e16ff-06e3-43eb-8d33-4416c2d75876  Leonardo Phoenix (photorealistic)
#   6bef9f1b-29cb-40c7-b9df-32b51c1f67d3  Leonardo Diffusion XL (stylised)
LEONARDO_MODEL_ID=

# ─────────────────────────────────────────────────────────────
# HOW TO GENERATE SITE IMAGES
# ─────────────────────────────────────────────────────────────
# 1. Create a free account at app.leonardo.ai
# 2. Go to Settings → API → Generate API Key
# 3. Paste the key as LEONARDO_API_KEY above
# 4. Run: pnpm generate:images           (generates all 8 images)
#    Or:  pnpm generate:images hero.jpg  (one specific image)
# 5. Images are saved to public/images/ and used automatically
# 6. Re-run any time you want to refresh a specific image
#
# Image slots generated:
#   hero.jpg              Home hero (1024×768)
#   sobre-team.jpg        About wide banner (1344×576)
#   sobre-step-1.jpg      About timeline — Antes (1024×576)
#   sobre-step-2.jpg      About timeline — Parceria LK (1024×576)
#   sobre-step-3.jpg      About timeline — Depois (1024×576)
#   solucoes-geo.jpg      Solutions — GEO & SEO (768×768)
#   solucoes-branding.jpg Solutions — Branding (768×768)
#   solucoes-automacao.jpg Solutions — Automação (768×768)
# ─────────────────────────────────────────────────────────────

# ── Contentful CMS ───────────────────────────────────────────
# Found at: https://app.contentful.com → Settings → API keys

# Your Contentful Space ID
CONTENTFUL_SPACE_ID=

# Content Delivery API access token (for published content)
CONTENTFUL_ACCESS_TOKEN=

# Content Preview API access token (for draft content — optional)
CONTENTFUL_PREVIEW_TOKEN=

# ─────────────────────────────────────────────────────────────
# HOW TO CONNECT CONTENTFUL
# ─────────────────────────────────────────────────────────────
# 1. Create a free account at contentful.com
# 2. Create a new Space (e.g. "LK Digital")
# 3. Go to Settings → API Keys → Add API Key
# 4. Copy Space ID and Content Delivery API access token above
# 5. In Contentful, create a Content Type with ID: blogPost
#    Add these fields:
#      title        — Short text  (required)
#      slug         — Short text  (required, unique)
#      description  — Short text  (required)
#      category     — Short text  (required)
#      readTime     — Short text  (required, e.g. "9 min")
#      tags         — Array of Short text
#      content      — Long text / Markdown (required)
#      coverImage   — Media  (optional)
#      publishedAt  — Date   (optional)
# 6. Restart the dev server after updating this file
# ─────────────────────────────────────────────────────────────
