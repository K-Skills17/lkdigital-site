# ─────────────────────────────────────────────────────────────
# LK Digital — Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git.
# ─────────────────────────────────────────────────────────────

# ── Contentful CMS ───────────────────────────────────────────
# Found at: https://app.contentful.com → Settings → API keys

# Your Contentful Space ID
CONTENTFUL_SPACE_ID=

# Content Delivery API access token (for published content)
CONTENTFUL_ACCESS_TOKEN=

# Content Preview API access token (for draft content — optional)
CONTENTFUL_PREVIEW_TOKEN=

# ─────────────────────────────────────────────────────────────
# HOW TO CONNECT CONTENTFUL
# ─────────────────────────────────────────────────────────────
# 1. Create a free account at contentful.com
# 2. Create a new Space (e.g. "LK Digital")
# 3. Go to Settings → API Keys → Add API Key
# 4. Copy Space ID and Content Delivery API access token above
# 5. In Contentful, create a Content Type with ID: blogPost
#    Add these fields:
#      title        — Short text  (required)
#      slug         — Short text  (required, unique)
#      description  — Short text  (required)
#      category     — Short text  (required)
#      readTime     — Short text  (required, e.g. "9 min")
#      tags         — Array of Short text
#      content      — Long text / Markdown (required)
#      coverImage   — Media  (optional)
#      publishedAt  — Date   (optional)
# 6. Restart the dev server after updating this file
# ─────────────────────────────────────────────────────────────
